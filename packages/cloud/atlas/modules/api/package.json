{
  "name": "@stt/atlas.cloud.atlas.api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --enable-source-maps dist/main.js",
    "dev": "bun --watch src/main.ts",
    "bundle": "bun build src/main.ts --outdir dist --target node --format esm",
    "webiai:bundle": "run-s bundle"
  },
  "dependencies": {
    "@stt/atlas.cloud.atlas": "file:../..",
    "@aws-sdk/client-dynamodb": "^3.958.0",
    "@aws-sdk/lib-dynamodb": "^3.958.0"
  }
}
